# Copy to .env and fill in. .env is gitignored — never commit real values.

# Absolute path to the private Obsidian vault project folder that holds the
# implementation plan + bookkeeping (implementation_plan_v0.1.md, overview.md, log.md, tasks.md).
VAULT_PROJECT_DIR="/absolute/path/to/Notebook/Lab/Synthetic Portraits"

# RunPod API key — needed only from Phase 5 (metered GPU). Leave blank until then.
RUNPOD_API_KEY=""
